<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <title>Latency Measure</title>
  </head>
  <body>
    <script src="https://code.jquery.com/jquery-3.4.1.slim.min.js" integrity="sha384-J6qa4849blE2+poT4WnyKhv5vZF5SrPo0iEjwBvKU7imGFAV0wwj1yYfoRSJoZ+n" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/js/bootstrap.min.js" integrity="sha384-wfSDF2E50Y2D1uUdj0O3uMBJnjuUD4Ih7YwaYd1iqfktj0Uod8GCExl3Og8ifwB6" crossorigin="anonymous"></script>
    <script src="/socket.io/socket.io.js"></script>
    <div class="container pl-0 pr-0">
      <div class="card" style="margin-top: 3rem;">
        <div class="row m-0 px-0 py-3">
          <div class="col-lg-12">
            <div class="entry-title h2">Measuring latency between hosts</div>
            <div id="pingcontainer">
              <p>Current Ping Client - Servers:</p>
              <script>
                const hosts = [
                  'http://hag241:3000',
                  'http://hag241:3001',
                ]
                const hostnames = [
                  'localhost',
                  'testhost'
                ]
                hostnames.forEach(function (host) {
                  var tag = document.createElement("span");
                  tag.setAttribute("id", `span${host}`)
                  tag.innerHTML=`<b>${host}</b>: .. ms `;
                  var parent = document.getElementById("pingcontainer");
                  parent.appendChild(tag);
                })
                
                var sockets = []
                hosts.forEach(function (host) {
                  sockets.push(io(host));
                })
                sockets.map(function (socket) {
                  setInterval(function() {
                  socket.emit('latency', Date.now(), function(startTime, host) {
                    var latency = Date.now() - startTime;
                    document.getElementById(`span${host}`).innerHTML = `<b>${host}</b>: ${latency} ms `;
                  });
                }, 5000);
                })
                
              </script>
            </div>
          </div>      
        </div>
        <div class="row m-0 px-0 py-0" style="max-height: 100%">
          <table class="table table-bordered">
            <thead>
              <tr>
                <th scope="col"></th>
                <% data.forEach(function (entry) { %> 
                  <th scope="col" style=""><%=entry.host%>
                  </th> 
                  <% }) %>      
              </tr>
            </thead>
            <tbody>
              <% data.forEach(function (entry) { %>
              <tr>
                <th scope="col"><%=entry.host%></th>
                <% entry.entries.forEach(function (value) { %>  
                  <% if(value.endpoint == entry.host) { %>
                    <td class="bg-secondary">&nbsp;
                    </td>
                    <% } else { %> 
                  <td><%=value.timings.total%>
                  </td> 
                  <% } %>
                <% }) %>
              </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </body>
</html>